import styled from "styled-components";

export const Section2Styled = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100vw;
    position: relative;
`;
